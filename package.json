{
  "name": "@elizaos/plugin-admin",
  "version": "1.5.0",
  "description": "Admin plugin for elevating privileges and performing global queries across the ElizaOS agent runtime.",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rimraf dist",
    "test": "vitest"
  },
  "keywords": [
    "elizaos",
    "plugin",
    "admin",
    "privileges"
  ],
  "author": "ElizaOS Core Team",
  "license": "Apache-2.0",
  "dependencies": {
    "@elizaos/core": "latest",
    "zod": "^4.0.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "tsup": "^8.5.0",
    "typescript": "^5.3.1",
    "rimraf": "^6.0.1",
    "vitest": "^3.2.4"
  }
} 